---
tags: [小程序, 昵称优化, 用户体验, 排行榜]
created: 2026-06-22
type: plan
status: active
---

#   排行榜昵称优化方案

> **问题**：排行榜显示"匿名施主"，用户体验差
>
> **目标**：自动获取微信昵称，提升排行榜真实感和社交性
>
> **当前状态**：15人参与修行，全部显示匿名施主

---

##   问题分析

### 当前实现

1. **用户昵称存储**：`users` 集合的 `nickName` 字段
2. **获取方式**：用户需手动在"我的"页面点击昵称输入框设置
3. **显示逻辑**：`getMeritRanking` 云函数中 `nickName || '匿名施主'`

### 问题根源

```
用户打开小程序 → 直接敲木鱼 → 不去"我的"页面设置昵称
                              ↓
                    排行榜显示"匿名施主"
```

**核心问题**：用户没有主动设置昵称的动机，且设置入口不明显。

---

##   解决方案

### 方案一：首次使用引导设置昵称（推荐 ⭐）

**原理**：用户首次打开小程序时，弹出昵称设置引导

**实现方式**：
1. 在 `index.js` 的 `onLoad` 中检测是否已设置昵称
2. 如果未设置，弹出模态框引导用户设置
3. 使用微信原生 `<input type="nickname">` 组件获取昵称

**优点**：
- 符合微信隐私政策
- 用户主动设置，体验自然
- 一次性设置，后续自动同步

**缺点**：
- 需要用户额外操作
- 部分用户可能跳过

---

### 方案二：排行榜页面提示设置昵称

**原理**：在排行榜页面显示提示，引导用户设置昵称

**实现方式**：
1. 在 `ranking.wxml` 顶部添加提示条
2. 如果用户未设置昵称，显示"点击设置昵称"
3. 点击后跳转到"我的"页面

**优点**：
- 实现简单
- 在关键场景（查看排行榜）提醒用户

**缺点**：
- 需要用户跳转页面
- 效果有限

---

### 方案三：自动获取微信昵称（不推荐 ⚠️）

**原理**：使用 `wx.getUserProfile()` 自动获取昵称

**问题**：
- 微信已废弃此 API，需要用户授权
- 审核可能被拒
- 不符合隐私政策

**结论**：不采用此方案

---

##   推荐方案：方案一 + 方案二 组合

### 实现步骤

#### 步骤1：添加昵称设置状态检测

在 `app.js` 中添加昵称设置状态检测：

```javascript
// 检查是否已设置昵称
checkNicknameSet() {
  const nickName = wx.getStorageSync('nickName');
  return nickName && nickName.length > 0;
}
```

#### 步骤2：首页首次使用引导

在 `index.js` 的 `onLoad` 中添加引导逻辑：

```javascript
onLoad(options) {
  this.syncFromGlobal();
  this.checkPrivacy();
  this.loadTodayMerit();
  this._autoTapSession = 0;
  this.loadCheckInState();
  this.updateRankTitle();
  this._setupShareMenu();

  // 追踪分享来源
  if (options && options.from) {
    this._fromShare = true;
  }

  // 新增：首次使用昵称设置引导
  this._showNicknameGuide();
},

// 显示昵称设置引导
_showNicknameGuide() {
  // 检查是否已设置昵称
  const nickName = wx.getStorageSync('nickName');
  if (nickName && nickName.length > 0) {
    return; // 已设置，不显示引导
  }

  // 检查是否已显示过引导
  const guideShown = wx.getStorageSync('nicknameGuideShown');
  if (guideShown) {
    return; // 已显示过，不重复显示
  }

  // 延迟显示，避免阻塞UI
  setTimeout(() => {
    wx.showModal({
      title: '欢迎来到电子木鱼 🙏',
      content: '设置一个昵称，让其他修行者认识你吧！',
      confirmText: '设置昵称',
      cancelText: '稍后设置',
      confirmColor: '#FFD700',
      success: (res) => {
        if (res.confirm) {
          // 跳转到"我的"页面设置昵称
          wx.switchTab({ url: '/pages/my/my' });
        }
        // 记录已显示过引导
        wx.setStorageSync('nicknameGuideShown', true);
      }
    });
  }, 2000); // 延迟2秒显示，让用户先体验小程序
}
```

#### 步骤3：排行榜页面提示

在 `ranking.wxml` 顶部添加提示条：

```html
<!-- 昵称设置提示 -->
<view class="nickname-tip" wx:if="{{!hasNickname}}">
  <text class="tip-text">设置昵称，让其他修行者认识你</text>
  <text class="tip-btn" bindtap="goToSetNickname">去设置</text>
</view>
```

在 `ranking.js` 中添加：

```javascript
data: {
  // ... 其他数据
  hasNickname: false
},

onShow() {
  this.loadRanking();
  this._checkNickname();
},

// 检查是否已设置昵称
_checkNickname() {
  const nickName = wx.getStorageSync('nickName');
  this.setData({
    hasNickname: nickName && nickName.length > 0
  });
},

// 跳转到设置昵称
goToSetNickname() {
  wx.switchTab({ url: '/pages/my/my' });
}
```

在 `ranking.wxss` 中添加样式：

```css
.nickname-tip {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 16rpx 24rpx;
  background: linear-gradient(135deg, #FFF8E1 0%, #FFECB3 100%);
  border-radius: 12rpx;
  margin-bottom: 20rpx;
}

.tip-text {
  font-size: 26rpx;
  color: #F57F17;
}

.tip-btn {
  font-size: 26rpx;
  color: #FF6F00;
  font-weight: bold;
}
```

#### 步骤4：自动同步昵称到云端

在 `app.js` 的 `syncToCloud` 方法中，确保昵称自动同步：

```javascript
syncToCloud(force = false) {
  this.globalData.pendingSyncMerit += 1;
  this.globalData.pendingSyncPower += 1;

  if (force || this.globalData.pendingSyncMerit >= 20) {
    const merit = this.globalData.pendingSyncMerit;
    const power = this.globalData.pendingSyncPower;
    this.globalData.pendingSyncMerit = 0;
    this.globalData.pendingSyncPower = 0;

    const data = { merit, power };

    // 附带头像和昵称（如果有变更）
    const userInfo = this.globalData.userInfo;
    if (userInfo) {
      if (userInfo.avatarUrl) data.avatarUrl = userInfo.avatarUrl;
      if (userInfo.nickName) data.nickName = userInfo.nickName;
    }

    // 确保昵称从本地存储同步
    const localNickName = wx.getStorageSync('nickName');
    if (localNickName && !data.nickName) {
      data.nickName = localNickName;
    }

    this.callCloudFn('addMerit', data).catch(err => {
      console.warn('[云同步] 同步失败（含重试）:', err.message || err);
      // 回滚增量到 pending 队列，下次敲击时会重新累积
      this.globalData.pendingSyncMerit += merit;
      this.globalData.pendingSyncPower += power;
      // 同时存入离线队列持久化，防止小程序被杀后数据丢失
      this._saveToOfflineQueue(data);
    });
  }
}
```

---

##   实现清单

### 需要修改的文件

| 文件 | 修改内容 | 优先级 |
|------|---------|:------:|
| `miniprogram/app.js` | 添加昵称设置状态检测 + 自动同步优化 | P0 |
| `miniprogram/pages/index/index.js` | 添加首次使用昵称设置引导 | P0 |
| `miniprogram/pages/ranking/ranking.js` | 添加昵称设置提示逻辑 | P1 |
| `miniprogram/pages/ranking/ranking.wxml` | 添加昵称设置提示UI | P1 |
| `miniprogram/pages/ranking/ranking.wxss` | 添加提示样式 | P1 |

### 测试要点

| 测试场景 | 预期结果 |
|---------|---------|
| 首次打开小程序 | 2秒后弹出昵称设置引导 |
| 已设置昵称用户 | 不弹出引导 |
| 排行榜页面（未设置昵称） | 顶部显示提示条 |
| 排行榜页面（已设置昵称） | 不显示提示条 |
| 设置昵称后 | 排行榜自动更新显示新昵称 |

---

##   版本规划

### v1.3.0 版本内容

1. ✅ 昵称设置引导优化
2. ✅ 排行榜提示条
3. ✅ 自动同步昵称到云端
4. ✅ 修复已知bug

### 预计效果

```
当前：15人参与，全部显示"匿名施主"
目标：15人参与，80%以上显示真实昵称
```

---

##   用户体验流程

```
用户首次打开小程序
    ↓
2秒后弹出引导："欢迎来到电子木鱼 🙏 设置一个昵称，让其他修行者认识你吧！"
    ↓
用户点击"设置昵称" → 跳转到"我的"页面
    ↓
用户在"我的"页面设置昵称（点击昵称输入框）
    ↓
昵称自动保存到本地 + 同步到云端
    ↓
排行榜显示真实昵称
```

---

##   注意事项

1. **微信隐私政策**：使用 `<input type="nickname">` 组件，符合最新隐私政策
2. **用户体验**：引导弹窗延迟2秒显示，避免阻塞用户首次体验
3. **容错处理**：如果用户跳过设置，后续在排行榜页面仍可提醒
4. **数据同步**：确保昵称在本地和云端保持一致

---

>   **记住**：用户设置昵称后，一定要及时同步到云端，确保排行榜显示最新数据。

---

> 📅 创建时间：2026-06-22
> 📅 最近更新：2026-06-22
> 🔄 下次更新：v1.3.0 发布后
